#pragma once
// Difficulty.h

// StrikeZone.h（新規）
struct StrikeZone
{
    Vector3 center = { -10.0f, 318.0f, 6000.0f };
    float halfWidth = 300.0f;   // ★ 実機で見ながら調整
    float halfHeight = 250.0f;  // ★ 実機で見ながら調整
};